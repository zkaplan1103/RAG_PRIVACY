# Decisions log — append one line per meaningful decision

Format: `YYYY-MM-DD | agent | decision`

---

2026-06-01 | orchestrator | Phase 0 scaffold complete: uv project, directory layout, .gitignore, pyproject.toml, source stubs (config, ingest, index, retrieve, generate, app.py), docs/CONTRACTS.md frozen.
2026-06-01 | orchestrator | Data sources selected: OPP-115 (usableprivacy.org, CC-NC research license), PrivacyQA (GitHub AbhilashaRavichander/PrivacyQA_EMNLP), PolicyQA (GitHub wasiahmad/PolicyQA). Raw data in data/raw/, git-ignored.
2026-06-01 | orchestrator | Embedding model: BAAI/bge-small-en-v1.5 (local, no API key needed). Generation model dev: claude-haiku-4-5; final: claude-sonnet-4-6.
2026-06-01 | orchestrator | score_floor=0.30 — below this for all top-k hits, answer() abstains.
2026-06-01 | orchestrator | OPP-115 acquired via GitHub mirror (pmayostendorp/beforeiaccept): 115 sanitized_policies HTML + 115 annotation CSVs in data/raw/opp115/. License: CC-NC, research/teaching only, cite Wilson et al. ACL 2016.
2026-06-01 | orchestrator | PrivacyQA acquired via git clone (AbhilashaRavichander/PrivacyQA_EMNLP): 185,200 train rows + 62,150 test rows in data/raw/privacyqa/data/. License: MIT.
2026-06-01 | orchestrator | torch pinned to 2.2.x, sentence-transformers to 2.7.x, onnxruntime overridden to 1.23.2 — macOS x86_64 has no wheels for newer onnxruntime/torch. Record in pyproject.toml [tool.uv] override-dependencies.
2026-06-01 | orchestrator | All 7 contract tests pass + 5/5 smoke test (stub mode) pass. Phase 0 complete. Ready for Phase 1.
2026-06-01 | data-engineer | Chunking strategy: merge adjacent same-category segments → split at sentence boundaries if >1600 chars (≈400 tokens). 115 policies → 2393 chunks.
2026-06-01 | index-engineer | numpy pinned to <2.0 for torch 2.2 ABI compat. Chroma score = 1 - cosine_distance. Cache via collection.count()>0.
2026-06-01 | rag-engineer | Two abstention paths: score_floor pre-LLM + UNANSWERABLE post-LLM. Citations from [N] markers in answer text.
2026-06-01 | ui-engineer | Sidebar policy dropdown (KNOWN_POLICIES dict). Phase 2 swap point marked with comment in app.py.
2026-06-01 | orchestrator | Phase 3 complete: load_golden() implemented for PrivacyQA (400 test items, 366 answerable / 34 unanswerable). eval/metrics.py interface stub documented. README written for non-technical reader.
2026-06-11 | orchestrator | Production upgrade Phase 0: CONTRACTS Part II (v2) frozen — Config v2, pgvector schema + hybrid/rerank rules, observability contract, eval v2 (GoldenItemV2, Ragas, gates), API contract, env var registry. Plan in docs/UPGRADE_PLAN.md.
2026-06-11 | orchestrator | v2 agent roster created in .claude/agents/ (eval/vector/observability/infra/docs-engineer); v1 builders retired. New memory tags: observability (06), infra (07). .claude/ root duplicates of CONTRACTS/INDEX converted to pointers (canonical = docs/).
2026-06-12 | orchestrator | P1 complete (user-approved): demo mode merged into app.py from "app 2.py" (no key → canned answers); stray duplicates deleted. Config v2 fields added + tests (tests/test_config_v2.py). Pre-existing lint/type debt cleared: ruff 0 (was 32), pyright 0 (was 30, data/ now excluded). User decision: baseline_v1 = eval suite on CHROMA path saved to eval/baselines/baseline_v1.json BEFORE pgvector becomes default (UPGRADE_PLAN decision 8). NOTE: test_retrieval.py takes ~30 min on CPU — CI must cache HF models.
2026-06-11 | orchestrator | Key decisions: Retriever protocol unchanged (backend via Config.retrieval_backend, chroma default); hybrid = RRF(cosine HNSW + tsvector FTS) + local bge-reranker-base; Lambda as container image; Ragas judge claude-opus-4-8; gates faithfulness>=0.80 + abstention>=0.90 (recalibrate after baseline); all integrations no-op without env vars.
2026-06-12 | eval-engineer | P2 complete: golden_v1.jsonl (167 items, 18% unanswerable), Ragas harness with --backend {chroma,pgvector} + --out PATH, eval/baselines/ README, metrics.py evaluate() implemented, thresholds.yaml set; 53 tests pass, ruff+pyright clean on eval files.
2026-06-12 | vector-engineer | PgVectorRetriever complete (CONTRACTS §7): RRF fusion + bge-reranker-base cross-encoder + [0,1] rescaling; make_retriever() factory in retrieve.py; migrate_pgvector.py idempotent backfill; 32 unit tests pass, docker/reranker tests skip cleanly. Chroma path unchanged.
2026-06-12 | observability-engineer | P3 complete: observability.py LangFuse wrapper (trace_answer CM + retrieve/rerank/generate spans); langfuse>=2.0.0,<3.0.0 added to deps; Config v2 fields added; strict no-op without keys; 27 tests green; rerank span hook defined for PgVectorRetriever.
2026-06-13 | infra-engineer | P5+P6 complete: api/handler.py (thin adapter, full input validation, policy_id allowlist via KNOWN_POLICY_IDS env/OPP-115 fallback, Secrets Manager cold-start, 400/404/500 shapes); api/Dockerfile (Lambda container, CPU torch, HF models baked in); infra/main.tf (ECR, Lambda, HTTP API v2, CloudWatch, Secrets Manager, least-privilege IAM — no wildcards); eval/gate.py (unit-tested gate script); .github/workflows/ci.yml (checks + eval jobs with HF+uv caching); 34 new tests green, fast suite 149/149 green.
2026-06-13 | docs-engineer | P7 complete: SETUP_TASKS.md (7 groups, contractual ordering — financial backstops first, baseline eval second, pgvector cutover gated on baseline_v1.json), TESTING_CHECKLIST.md (9 checks including auth/validation negative tests and broken-gate proof), README.md updated with production architecture section (diagram, cost, security posture). License table intact. Gap: run_ragas.py --backend/--out flags confirmed present; baseline_v1.json correctly documented as pending user action.
